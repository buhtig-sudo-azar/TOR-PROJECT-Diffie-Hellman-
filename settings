# ...or create a new repository on the command line
echo "# TOR-PROJECT---Diffie-Hellman-" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/buhtig-sudo-azar/TOR-PROJECT---Diffie-Hellman-.git
git push -u origin main
bash
# ...or push an existing repository from the command line
git remote set-url origin https://github.com/buhtig-sudo-azar/TOR-PROJECT---Diffie-Hellman-.git
git branch -M main
git push -u origin main